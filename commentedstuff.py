# end = dt.datetime.strftime(start + dt.timedelta(minutes = 15), frmt)
# end = start + dt.timedelta(minutes = 15)
# print dt.datetime.strftime(a[0], frmt)
# t = [sample_date - dt.timedelta(minutes=x) for x in range(3, 35)]
# stime = dt.datetime.strptime(start, frmt)
# etime = dt.datetime.strptime(end, frmt)
# a.append(dt.datetime.strftime(sample_date, "%Y-%m-%d %H:%M:%S"))